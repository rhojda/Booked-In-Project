<h1>{{book.title}}</h1>
{{#if authors}}
<p>
    Written by {{#each authors}}{{this.firstName}} {{this.lastName}}{{#unless @last}}, {{/unless}}{{/each}}
</p>
{{/if}}
<p>
    Originally published in {{book.publishingYear}}
</p>
{{#if genres}}
<p>
    Genre is {{#each genres}}{{this.genre_title}} {{#unless @last}}, {{/unless}}{{/each}}
</p>
{{/if}}
<h1>{{book.title}}</h1>
{{#if author}} {{!/Check if we have an author set}}
<p>Written by {{#each authors}}{{this.firstName}} {{this.lastName}}{{#unless @last}}, {{/unless}}{{/each}}</p>
{{/if}}
<p>
    Originally published in {{book.publishingYear}}
</p>